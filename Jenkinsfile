pipeline {
    agent any
    stages {
      stage('Build') {
        steps {
          echo "Hellooooo!"
          }
      stage('Build') {
        steps {
          echo "AWS!"
          }
        }
      }
    }
}
